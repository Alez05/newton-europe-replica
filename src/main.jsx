import React from 'react';
import ReactDOM from 'react-dom/client';
import { Newton } from './component';

ReactDOM.createRoot(document.getElementById('here')).render(
  <React.StrictMode>
    <Newton />
  </React.StrictMode>
);
